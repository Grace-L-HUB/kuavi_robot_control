# 语音识别后端。
# PC 开发: whisper_local
# Atlas NPU: ascend_om（需 whisper_encoder.om，见 scripts/convert_whisper_encoder_to_om.sh）
asr:
  backend: whisper_local # whisper_local | ascend_om
  whisper:
    model_size: tiny # tiny, base, small, ...
    language: zh # 可设为 null 让 Whisper 自检测
  # ascend:
  #   encoder_om: ascend_models/whisper_encoder.om
  #   model_size: tiny
  #   language: zh
  #   device_id: 0
